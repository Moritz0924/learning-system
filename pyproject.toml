[project]
name = "adaptive-private-tutor-stage1"
version = "0.1.0"
description = "Stage 1 backend for the adaptive private tutor learning loop."
requires-python = ">=3.10"
dependencies = [
    "alembic>=1.13",
    "celery[redis]>=5.4",
    "fastapi>=0.110",
    "httpx>=0.27",
    "langgraph>=1.2",
    "langgraph-checkpoint-postgres>=3.1,<4",
    "mcp[cli]>=1.27,<3",
    "minio>=7.2",
    "pillow>=10.0",
    "psycopg[binary]>=3.2",
    "pytesseract>=0.3",
    "pwdlib[argon2]>=0.2",
    "PyJWT>=2.8",
    "email-validator>=2.2",
    "pypdf>=4",
    "PyMuPDF>=1.24",
    "python-multipart>=0.0.9",
    "python-pptx>=1.0",
    "pydantic>=2.7",
    "sqlalchemy>=2.0",
    "uvicorn>=0.29",
]

[project.optional-dependencies]
dev = [
    "jsonschema>=4.20",
    "pyyaml>=6.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
where = [".", "src"]
include = ["backend*", "adaptive_tutor*"]

[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"
